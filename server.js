"use strict";
/**
 * Hostinger lsnode require() entry — must stay CommonJS (no top-level import/await).
 * hPanel is configured with entry file server.js; implementation lives in server.cjs.
 */
require("./server.cjs");
