"use strict";
/**
 * Hostinger lsnode require() entry — must stay CommonJS (no top-level import/await).
 * hPanel may use server.js or server.cjs; implementation lives in server.cjs.
 */
require("./server.cjs");
