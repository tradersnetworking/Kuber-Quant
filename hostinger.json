{
  "framework": "express",
  "nodeVersion": "20",
  "packageManager": "npm",
  "branch": "main",
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "startCommand": "node server.js",
  "outputDirectory": "artifacts/trading-platform/dist/public",
  "entryFile": "server.js",
  "notes": "GitHub deploy: connect repo tradersnetworking/Kuber-Quant, branch main, package manager npm, entry server.js. npm install runs postinstall (Hostinger-safe pnpm bootstrap). npm run build compiles web+API. Do not set PORT in env. Import hostinger.env.example via hPanel."
}
