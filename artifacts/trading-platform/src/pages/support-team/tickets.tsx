import { SupportTicketsWorkspace } from "@/components/support/SupportTicketsWorkspace";

export default function SupportTeamTickets() {
  return (
    <SupportTicketsWorkspace
      title="Support Tickets"
      description="View, reply to, and resolve all customer support tickets across the platform."
    />
  );
}
